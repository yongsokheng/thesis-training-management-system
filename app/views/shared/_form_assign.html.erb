<div class="row">
  <div class="col-md-12">
    <table id="tbl-assign-user" class="table table-hover">
      <thead>
        <th class="assign-checkbox"></th>
        <th><%= t "courses.users.name" %></th>
        <th><%= t "courses.users.email" %></th>
      </thead>
      <tbody>
        <%= render partial: "shared/assign_user_row", collection: users,
          as: :user, locals: {f: f, course_users: course_users} %>
      </tbody>
    </table>
  </div>
</div>
<br>
<div class="row">
  <div class="col-md-1 col-md-offset-11">
  <%= f.submit :save, class: "btn btn-primary pull-right" %>
  </div>
</div>

