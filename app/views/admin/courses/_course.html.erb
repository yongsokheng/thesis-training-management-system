<div class="course_record row">
  <hr>
  <div id="course-<%= course.id %>">
    <div class="col-md-10">
      <h4><%= link_to course.name, admin_course_path(course) %></h4>
      <p><%= course.description %></p>
    </div>
  </div>
</div>
